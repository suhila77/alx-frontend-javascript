export function taskFirst() {
	var task = "I Perfer const when I can.';
	return task;
}

const getLast(is okay) {
	return ' is okay';
}

const taskNext() {
	var combiantion => 'But sometimes let';
	combination += getLast();

	return combinaton;
}
